services:
  content-service:
    build:
      context: ./content-service
      dockerfile: Dockerfile
    container_name: speakasap-content-green
    ports:
      - "${CONTENT_SERVICE_PORT_GREEN}:${CONTENT_SERVICE_PORT}"
    env_file:
      - .env
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - DOMAIN=${DOMAIN}
      - SERVICE_NAME=${SERVICE_NAME}
      - PORT=${CONTENT_SERVICE_PORT}
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${DB_NAME}
      - DATABASE_URL=${DATABASE_URL}
      - PRISMA_SCHEMA_ENGINE_BINARY=${PRISMA_SCHEMA_ENGINE_BINARY:-}
      - PRISMA_QUERY_ENGINE_BINARY=${PRISMA_QUERY_ENGINE_BINARY:-}
      - REDIS_HOST=${REDIS_HOST:-}
      - REDIS_PORT=${REDIS_PORT:-}
      - REDIS_PASSWORD=${REDIS_PASSWORD:-}
      - REDIS_DB=${REDIS_DB:-}
      - LOG_LEVEL=${LOG_LEVEL}
      - LOG_TIMESTAMP_FORMAT=${LOG_TIMESTAMP_FORMAT}
      - LOGGING_SERVICE_URL=${LOGGING_SERVICE_URL}
      - LOGGING_SERVICE_API_PATH=${LOGGING_SERVICE_API_PATH}
      - LOGGING_SERVICE_TIMEOUT=${LOGGING_SERVICE_TIMEOUT}
      - AUTH_SERVICE_URL=${AUTH_SERVICE_URL}
      - NOTIFICATION_SERVICE_URL=${NOTIFICATION_SERVICE_URL}
      - NOTIFICATION_SERVICE_TIMEOUT=${NOTIFICATION_SERVICE_TIMEOUT}
      - NOTIFICATION_RETRY_MAX_ATTEMPTS=${NOTIFICATION_RETRY_MAX_ATTEMPTS}
      - NOTIFICATION_RETRY_DELAY_MS=${NOTIFICATION_RETRY_DELAY_MS}
      - HTTP_TIMEOUT=${HTTP_TIMEOUT}
      - GATEWAY_TIMEOUT=${GATEWAY_TIMEOUT}
      - AUTH_SERVICE_TIMEOUT=${AUTH_SERVICE_TIMEOUT}
      - RETRY_MAX_ATTEMPTS=${RETRY_MAX_ATTEMPTS}
      - RETRY_DELAY_MS=${RETRY_DELAY_MS}
      - AUTH_RETRY_MAX_ATTEMPTS=${AUTH_RETRY_MAX_ATTEMPTS}
      - AUTH_RETRY_DELAY_MS=${AUTH_RETRY_DELAY_MS}
      - CORS_ORIGIN=${CORS_ORIGIN}
      - FRONTEND_URL=${FRONTEND_URL}
      - AI_SERVICE_URL=${AI_SERVICE_URL}
      - AI_SERVICE_TIMEOUT=${AI_SERVICE_TIMEOUT}
      - AI_SERVICE_API_KEY=${AI_SERVICE_API_KEY}
      - AI_SERVICE_TRANSLATE_PATH=${AI_SERVICE_TRANSLATE_PATH}
      - DEFAULT_PAGE_SIZE=${DEFAULT_PAGE_SIZE}
      - MAX_PAGE_SIZE=${MAX_PAGE_SIZE}
      - ASSETS_BASE_URL=${ASSETS_BASE_URL}
    networks:
      - nginx-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:${CONTENT_SERVICE_PORT}/health"]
      interval: 10s
      timeout: 10s
      retries: 2

  certification-service:
    build:
      context: ./certification-service
      dockerfile: Dockerfile
    container_name: speakasap-certification-green
    ports:
      - "${CERTIFICATION_SERVICE_PORT_GREEN:-4202}:${CERTIFICATION_SERVICE_PORT:-4202}"
    env_file:
      - .env
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - DOMAIN=${CERTIFICATION_SERVICE_DOMAIN:-speakasap-certification.alfares.cz}
      - SERVICE_NAME=${CERTIFICATION_SERVICE_NAME:-certification-service}
      - PORT=${CERTIFICATION_SERVICE_PORT:-4202}
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${CERTIFICATION_DB_NAME:-speakasap_certification_db}
      - DATABASE_URL=${CERTIFICATION_DATABASE_URL:-postgresql://dbadmin@db-server-postgres:5432/speakasap_certification_db}
      - REDIS_HOST=${REDIS_HOST:-}
      - REDIS_PORT=${REDIS_PORT:-}
      - REDIS_PASSWORD=${REDIS_PASSWORD:-}
      - REDIS_DB=${REDIS_DB:-}
      - LOG_LEVEL=${LOG_LEVEL}
      - LOG_TIMESTAMP_FORMAT=${LOG_TIMESTAMP_FORMAT}
      - LOGGING_SERVICE_URL=${LOGGING_SERVICE_URL}
      - LOGGING_SERVICE_API_PATH=${LOGGING_SERVICE_API_PATH}
      - LOGGING_SERVICE_TIMEOUT=${LOGGING_SERVICE_TIMEOUT}
      - HTTP_TIMEOUT=${HTTP_TIMEOUT}
      - GATEWAY_TIMEOUT=${GATEWAY_TIMEOUT}
      - AUTH_SERVICE_URL=${AUTH_SERVICE_URL}
      - AUTH_SERVICE_TIMEOUT=${AUTH_SERVICE_TIMEOUT}
      - JWT_SECRET=${JWT_SECRET}
      - CORS_ORIGIN=${CORS_ORIGIN}
      - FRONTEND_URL=${FRONTEND_URL}
    networks:
      - nginx-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:${CERTIFICATION_SERVICE_PORT:-4202}/health"]
      interval: 10s
      timeout: 10s
      retries: 2

  assessment-service:
    build:
      context: ./assessment-service
      dockerfile: Dockerfile
    container_name: speakasap-assessment-green
    ports:
      - "${ASSESSMENT_SERVICE_PORT_GREEN:-4203}:${ASSESSMENT_SERVICE_PORT:-4203}"
    env_file:
      - .env
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - DOMAIN=${ASSESSMENT_SERVICE_DOMAIN:-speakasap-assessment.alfares.cz}
      - SERVICE_NAME=${ASSESSMENT_SERVICE_NAME:-assessment-service}
      - PORT=${ASSESSMENT_SERVICE_PORT:-4203}
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${ASSESSMENT_DB_NAME:-speakasap_assessment_db}
      - DATABASE_URL=${ASSESSMENT_DATABASE_URL:-postgresql://dbadmin@db-server-postgres:5432/speakasap_assessment_db}
      - REDIS_HOST=${REDIS_HOST:-}
      - REDIS_PORT=${REDIS_PORT:-}
      - REDIS_PASSWORD=${REDIS_PASSWORD:-}
      - REDIS_DB=${REDIS_DB:-}
      - LOG_LEVEL=${LOG_LEVEL}
      - LOG_TIMESTAMP_FORMAT=${LOG_TIMESTAMP_FORMAT}
      - LOGGING_SERVICE_URL=${LOGGING_SERVICE_URL}
      - LOGGING_SERVICE_API_PATH=${LOGGING_SERVICE_API_PATH}
      - LOGGING_SERVICE_TIMEOUT=${LOGGING_SERVICE_TIMEOUT}
      - HTTP_TIMEOUT=${HTTP_TIMEOUT}
      - GATEWAY_TIMEOUT=${GATEWAY_TIMEOUT}
      - AUTH_SERVICE_URL=${AUTH_SERVICE_URL}
      - AUTH_SERVICE_TIMEOUT=${AUTH_SERVICE_TIMEOUT}
      - LANGUAGE_TEST_LANDING_BASE_URL=${LANGUAGE_TEST_LANDING_BASE_URL:-https://speakasap.alfares.cz}
      - ASSESSMENT_SERVICE_PUBLIC_BASE_URL=${ASSESSMENT_SERVICE_PUBLIC_BASE_URL:-https://speakasap-assessment.alfares.cz}
      - ASSESSMENT_VIEW_TOKEN_SECRET=${ASSESSMENT_VIEW_TOKEN_SECRET}
      - USER_TEST_ASSETS_DIR=${USER_TEST_ASSETS_DIR:-/app/assets}
      - CORS_ORIGIN=${CORS_ORIGIN}
      - FRONTEND_URL=${FRONTEND_URL}
    networks:
      - nginx-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:${ASSESSMENT_SERVICE_PORT:-4203}/health"]
      interval: 10s
      timeout: 10s
      retries: 2

  user-service:
    build:
      context: ./user-service
      dockerfile: Dockerfile
    container_name: speakasap-user-green
    ports:
      - "${USER_SERVICE_PORT_GREEN}:${USER_SERVICE_PORT}"
    env_file:
      - .env
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - DOMAIN=${DOMAIN}
      - SERVICE_NAME=${USER_SERVICE_NAME}
      - PORT=${USER_SERVICE_PORT}
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${USER_DB_NAME}
      - DATABASE_URL=${USER_DATABASE_URL}
      - REDIS_HOST=${REDIS_HOST:-}
      - REDIS_PORT=${REDIS_PORT:-}
      - REDIS_PASSWORD=${REDIS_PASSWORD:-}
      - REDIS_DB=${REDIS_DB:-}
      - LOG_LEVEL=${LOG_LEVEL}
      - LOG_TIMESTAMP_FORMAT=${LOG_TIMESTAMP_FORMAT}
      - LOGGING_SERVICE_URL=${LOGGING_SERVICE_URL}
      - LOGGING_SERVICE_API_PATH=${LOGGING_SERVICE_API_PATH}
      - LOGGING_SERVICE_TIMEOUT=${LOGGING_SERVICE_TIMEOUT}
      - HTTP_TIMEOUT=${HTTP_TIMEOUT}
      - GATEWAY_TIMEOUT=${GATEWAY_TIMEOUT}
      - CORS_ORIGIN=${CORS_ORIGIN}
      - FRONTEND_URL=${FRONTEND_URL}
    networks:
      - nginx-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:${USER_SERVICE_PORT}/health"]
      interval: 10s
      timeout: 10s
      retries: 2

  course-service:
    build:
      context: ./course-service
      dockerfile: Dockerfile
    container_name: speakasap-course-green
    ports:
      - "${COURSE_SERVICE_PORT_GREEN}:${COURSE_SERVICE_PORT}"
    env_file:
      - .env
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - DOMAIN=${DOMAIN}
      - SERVICE_NAME=${COURSE_SERVICE_NAME}
      - PORT=${COURSE_SERVICE_PORT}
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${COURSE_DB_NAME}
      - DATABASE_URL=${COURSE_DATABASE_URL}
      - REDIS_HOST=${REDIS_HOST:-}
      - REDIS_PORT=${REDIS_PORT:-}
      - REDIS_PASSWORD=${REDIS_PASSWORD:-}
      - REDIS_DB=${REDIS_DB:-}
      - LOG_LEVEL=${LOG_LEVEL}
      - LOG_TIMESTAMP_FORMAT=${LOG_TIMESTAMP_FORMAT}
      - LOGGING_SERVICE_URL=${LOGGING_SERVICE_URL}
      - LOGGING_SERVICE_API_PATH=${LOGGING_SERVICE_API_PATH}
      - LOGGING_SERVICE_TIMEOUT=${LOGGING_SERVICE_TIMEOUT}
      - AUTH_SERVICE_URL=${AUTH_SERVICE_URL}
      - AUTH_SERVICE_TIMEOUT=${AUTH_SERVICE_TIMEOUT}
      - HTTP_TIMEOUT=${HTTP_TIMEOUT}
      - GATEWAY_TIMEOUT=${GATEWAY_TIMEOUT}
      - DEFAULT_PAGE_SIZE=${DEFAULT_PAGE_SIZE}
      - MAX_PAGE_SIZE=${MAX_PAGE_SIZE}
      - INTERNAL_API_TOKEN=${INTERNAL_API_TOKEN}
      - CORS_ORIGIN=${CORS_ORIGIN}
      - FRONTEND_URL=${FRONTEND_URL}
    networks:
      - nginx-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:${COURSE_SERVICE_PORT}/health"]
      interval: 10s
      timeout: 10s
      retries: 2

  education-service:
    build:
      context: ./education-service
      dockerfile: Dockerfile
    container_name: speakasap-education-green
    ports:
      - "${EDUCATION_SERVICE_PORT_GREEN}:${EDUCATION_SERVICE_PORT}"
    env_file:
      - .env
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - DOMAIN=${DOMAIN}
      - SERVICE_NAME=${EDUCATION_SERVICE_NAME}
      - PORT=${EDUCATION_SERVICE_PORT}
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${EDUCATION_DB_NAME}
      - DATABASE_URL=${EDUCATION_DATABASE_URL}
      - REDIS_HOST=${REDIS_HOST:-}
      - REDIS_PORT=${REDIS_PORT:-}
      - REDIS_PASSWORD=${REDIS_PASSWORD:-}
      - REDIS_DB=${REDIS_DB:-}
      - LOG_LEVEL=${LOG_LEVEL}
      - LOG_TIMESTAMP_FORMAT=${LOG_TIMESTAMP_FORMAT}
      - LOGGING_SERVICE_URL=${LOGGING_SERVICE_URL}
      - LOGGING_SERVICE_API_PATH=${LOGGING_SERVICE_API_PATH}
      - LOGGING_SERVICE_TIMEOUT=${LOGGING_SERVICE_TIMEOUT}
      - AUTH_SERVICE_URL=${AUTH_SERVICE_URL}
      - AUTH_SERVICE_TIMEOUT=${AUTH_SERVICE_TIMEOUT}
      - HTTP_TIMEOUT=${HTTP_TIMEOUT}
      - GATEWAY_TIMEOUT=${GATEWAY_TIMEOUT}
      - DEFAULT_PAGE_SIZE=${DEFAULT_PAGE_SIZE}
      - MAX_PAGE_SIZE=${MAX_PAGE_SIZE}
      - INTERNAL_API_TOKEN=${INTERNAL_API_TOKEN}
      - CORS_ORIGIN=${CORS_ORIGIN}
      - FRONTEND_URL=${FRONTEND_URL}
    networks:
      - nginx-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:${EDUCATION_SERVICE_PORT}/health"]
      interval: 10s
      timeout: 10s
      retries: 2

  payment-service:
    build:
      context: ./payment-service
      dockerfile: Dockerfile
    container_name: speakasap-payment-green
    ports:
      - "${PAYMENT_SERVICE_PORT_GREEN:-4208}:${PAYMENT_SERVICE_PORT}"
    env_file:
      - .env
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - DOMAIN=${DOMAIN}
      - SERVICE_NAME=speakasap-payment-service
      - PORT=${PAYMENT_SERVICE_PORT}
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${PAYMENT_DB_NAME}
      - DATABASE_URL=${PAYMENT_DATABASE_URL}
      - PAYMENT_SERVICE_PORT=${PAYMENT_SERVICE_PORT}
      - PAYMENT_DB_NAME=${PAYMENT_DB_NAME}
      - PAYMENT_DATABASE_URL=${PAYMENT_DATABASE_URL}
      - PAYMENTS_MICROSERVICE_URL=${PAYMENTS_MICROSERVICE_URL}
      - LOG_LEVEL=${LOG_LEVEL}
      - LOG_TIMESTAMP_FORMAT=${LOG_TIMESTAMP_FORMAT}
      - LOGGING_SERVICE_URL=${LOGGING_SERVICE_URL}
      - LOGGING_SERVICE_API_PATH=${LOGGING_SERVICE_API_PATH}
      - LOGGING_SERVICE_TIMEOUT=${LOGGING_SERVICE_TIMEOUT}
      - AUTH_MICROSERVICE_URL=${AUTH_MICROSERVICE_URL}
      - AUTH_MICROSERVICE_PORT=${AUTH_MICROSERVICE_PORT}
      - CORS_ORIGIN=${CORS_ORIGIN}
      - FRONTEND_URL=${FRONTEND_URL}
    networks:
      - nginx-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:${PAYMENT_SERVICE_PORT}/health"]
      interval: 10s
      timeout: 10s
      retries: 2

  salary-service:
    build:
      context: ./salary-service
      dockerfile: Dockerfile
    container_name: speakasap-salary-green
    ports:
      - "${SALARY_SERVICE_PORT_GREEN:-4212}:${SALARY_SERVICE_PORT}"
    env_file:
      - .env
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - DOMAIN=${DOMAIN}
      - SERVICE_NAME=speakasap-salary-service
      - PORT=${SALARY_SERVICE_PORT}
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${SALARY_DB_NAME}
      - DATABASE_URL=${SALARY_DATABASE_URL}
      - SALARY_SERVICE_PORT=${SALARY_SERVICE_PORT}
      - SALARY_DB_NAME=${SALARY_DB_NAME}
      - SALARY_DATABASE_URL=${SALARY_DATABASE_URL}
      - LOG_LEVEL=${LOG_LEVEL}
      - LOG_TIMESTAMP_FORMAT=${LOG_TIMESTAMP_FORMAT}
      - LOGGING_SERVICE_URL=${LOGGING_SERVICE_URL}
      - LOGGING_SERVICE_API_PATH=${LOGGING_SERVICE_API_PATH}
      - LOGGING_SERVICE_TIMEOUT=${LOGGING_SERVICE_TIMEOUT}
      - AUTH_SERVICE_URL=${AUTH_SERVICE_URL}
      - AUTH_MICROSERVICE_URL=${AUTH_MICROSERVICE_URL}
      - AUTH_SERVICE_TIMEOUT=${AUTH_SERVICE_TIMEOUT}
      - AUTH_MICROSERVICE_PORT=${AUTH_MICROSERVICE_PORT}
      - CORS_ORIGIN=${CORS_ORIGIN}
      - FRONTEND_URL=${FRONTEND_URL}
    networks:
      - nginx-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:${SALARY_SERVICE_PORT}/health"]
      interval: 10s
      timeout: 10s
      retries: 2

  financial-service:
    build:
      context: ./financial-service
      dockerfile: Dockerfile
    container_name: speakasap-financial-green
    ports:
      - "${FINANCIAL_SERVICE_PORT_GREEN:-4213}:${FINANCIAL_SERVICE_PORT}"
    env_file:
      - .env
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - DOMAIN=${DOMAIN}
      - SERVICE_NAME=speakasap-financial-service
      - PORT=${FINANCIAL_SERVICE_PORT}
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${FINANCIAL_DB_NAME}
      - DATABASE_URL=${FINANCIAL_DATABASE_URL}
      - FINANCIAL_SERVICE_PORT=${FINANCIAL_SERVICE_PORT}
      - FINANCIAL_DB_NAME=${FINANCIAL_DB_NAME}
      - FINANCIAL_DATABASE_URL=${FINANCIAL_DATABASE_URL}
      - LOG_LEVEL=${LOG_LEVEL}
      - LOG_TIMESTAMP_FORMAT=${LOG_TIMESTAMP_FORMAT}
      - LOGGING_SERVICE_URL=${LOGGING_SERVICE_URL}
      - LOGGING_SERVICE_API_PATH=${LOGGING_SERVICE_API_PATH}
      - LOGGING_SERVICE_TIMEOUT=${LOGGING_SERVICE_TIMEOUT}
      - CORS_ORIGIN=${CORS_ORIGIN}
      - FRONTEND_URL=${FRONTEND_URL}
    networks:
      - nginx-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:${FINANCIAL_SERVICE_PORT}/health"]
      interval: 10s
      timeout: 10s
      retries: 2

  notification-service:
    build:
      context: ./notification-service
      dockerfile: Dockerfile
    container_name: speakasap-notification-green
    ports:
      - "${NOTIFICATION_SERVICE_PORT_GREEN:-4209}:${NOTIFICATION_SERVICE_PORT}"
    env_file:
      - .env
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - DOMAIN=${DOMAIN}
      - SERVICE_NAME=speakasap-notification-service
      - PORT=${NOTIFICATION_SERVICE_PORT}
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DATABASE_URL=${NOTIFICATION_DATABASE_URL}
      - NOTIFICATION_SERVICE_PORT=${NOTIFICATION_SERVICE_PORT}
      - NOTIFICATION_DATABASE_URL=${NOTIFICATION_DATABASE_URL}
      - NOTIFICATION_SERVICE_URL=${NOTIFICATION_SERVICE_URL}
      - LOG_LEVEL=${LOG_LEVEL}
      - LOG_TIMESTAMP_FORMAT=${LOG_TIMESTAMP_FORMAT}
      - LOGGING_SERVICE_URL=${LOGGING_SERVICE_URL}
      - LOGGING_SERVICE_API_PATH=${LOGGING_SERVICE_API_PATH}
      - LOGGING_SERVICE_TIMEOUT=${LOGGING_SERVICE_TIMEOUT}
      - CORS_ORIGIN=${CORS_ORIGIN}
      - FRONTEND_URL=${FRONTEND_URL}
    networks:
      - nginx-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:${NOTIFICATION_SERVICE_PORT}/health"]
      interval: 10s
      timeout: 10s
      retries: 2

networks:
  nginx-network:
    external: true
    name: nginx-network
