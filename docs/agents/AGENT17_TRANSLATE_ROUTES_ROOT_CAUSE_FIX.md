# AGENT17: Fix Content-Service Translate Routes 404 (Root Cause)

## Role

Senior Backend/Deployment Debug Agent for SpeakASAP Content Service.

## Objective

Fix the root cause of production `404` on:

- `POST /api/v1/dictionary/translate`
- `POST /api/v1/grammar/translate`

Do not patch symptoms. Restore correct route availability in deployed runtime.

## Current Evidence

- `GET https://content.alfares.cz/health` -> `200`
- `POST https://content.alfares.cz/api/v1/dictionary/translate` -> `404` (`Cannot POST ...`)
- `POST https://content.alfares.cz/api/v1/grammar/translate` -> `404` (`Cannot POST ...`)
- Codebase contains `@Post('translate')` in both controllers.

## Scope

- Diagnose code-to-runtime drift causing missing POST routes.
- Fix service code/deployment config in `speakasap` only.
- Add/ensure detailed logging with timestamps and `duration_ms` around translate flow and startup route registration evidence.
- Re-validate endpoints and error-path behavior.
- Update Phase 1 validation docs after fix.

## Hard Constraints

- Do **not** change nginx config (it is regenerated by deploy scripts).
- Do **not** increase timeouts to hide delays.
- Do **not** modify shared microservices (`nginx-microservice`, `logging-microservice`, `auth-microservice`, `database-server`).
- Do **not** create new scripts/tests unless absolutely required; prefer existing code paths.
- Keep `.env` as source of truth; no hardcoded URLs/keys.

## Investigation Checklist (Root Cause First)

1. Verify module wiring and controller registration for dictionary/grammar translation routes.
2. Verify global prefix/versioning and actual deployed route paths.
3. Verify deployed artifact/image matches current source commit (detect stale image/container drift).
4. Verify runtime environment values used by AI integration and route guards/interceptors.
5. Check runtime logs for startup route map and request traces with timestamps.
6. Confirm no conditional boot path excludes translate handlers in production mode.

## Implementation Requirements

- If the issue is code wiring:
  - Fix module/controller/provider registration.
  - Ensure both translate handlers are reachable under `/api/v1/...`.
- If the issue is deployment drift:
  - Fix deploy flow so updated service artifact is actually running.
  - Document exact drift cause (example: stale image tag, wrong compose target, wrong service path).
- Ensure translation error handling is explicit for:
  - AI unavailable
  - timeout
  - malformed payload

## Required Logging

- Startup: log service version/build metadata and registered translate routes.
- Per translate request: start/end log with timestamp, `duration_ms`, target language, and outcome status.
- On failure: include categorized reason (`timeout`, `unavailable`, `validation_error`, `unexpected_error`).

## Verification (Must Run)

1. Build:
   - `cd /Users/sergiystashok/Documents/GitHub/speakasap/content-service`
   - `npm run build`
2. Runtime checks:
   - `curl -sS -m 10 https://content.alfares.cz/health`
   - `curl -sS -m 15 -X POST https://content.alfares.cz/api/v1/dictionary/translate -H "Content-Type: application/json" -d '{"text":"Hello","targetLanguage":"cs"}'`
   - `curl -sS -m 15 -X POST https://content.alfares.cz/api/v1/grammar/translate -H "Content-Type: application/json" -d '{"text":"I am here","targetLanguage":"cs"}'`
3. Negative paths:
   - invalid payload -> expected `400` contract error shape
   - AI timeout/unavailable -> expected mapped error response shape
4. Confirm logs contain timestamped entries and `duration_ms`.

## Deliverables

1. Code/config changes that remove `404` root cause.
2. Brief root-cause note (`what was broken`, `why`, `what changed`).
3. Updated docs:
   - `docs/refactoring/PHASE1_VALIDATION_REPORT.md` (AI section + decision)
   - `docs/refactoring/CONTENT_CUTOVER_CHECKLIST.md` (mark blocker resolved if verified)
   - `docs/refactoring/PHASE1_COMPLETION_SUMMARY.md` (Phase 1 readiness status)
4. Final GO/NO-GO recommendation with evidence.

## Definition of Done

- Both translate endpoints return non-404 and execute service logic in production runtime.
- Error handling follows contract shape.
- Logging evidence with timestamps + `duration_ms` is present.
- Validation docs updated with fresh probe outputs.
