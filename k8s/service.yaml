apiVersion: v1
kind: Service
metadata:
  name: speakasap
  namespace: statex-apps
  labels:
    app: speakasap
    managed-by: k8s-migration
spec:
  type: ClusterIP
  selector:
    app: speakasap
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      name: http
